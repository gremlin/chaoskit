import React from 'react';

import FoundationLayout from '../layouts/Foundation';

const IndexDocs = () => (
  <FoundationLayout pageTitle="Overview">
    <p>ChaosKit is a lightweight and modular front-end framework for developing fast and powerful web interfaces within Gremlin.</p>

    <p>Emphasis is placed on base-styles that follow our <a href="/sass/">Sass guidelines</a> with the flexibility of overriding portions to your needs with hooks, class modifiers, and variable overrides that make this framework extensible to fit both marketing and application initiatives.</p>
  </FoundationLayout>
);

export default IndexDocs;
