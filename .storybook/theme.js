import { create } from '@storybook/theming/create'

import logo from './logo.svg'

export default create({
  base: 'light',
  brandTitle: 'ChaosKit',
  brandUrl: 'https://chaoskit.netlify.app/',
  brandImage: logo
})
